# Contributing Guide

## 커밋 메세지 규칙

모든 팀원은 아래 규칙에 따라 커밋 메세지를 작성합니다.

### 형식

```
[타입]: 제목

본문 (선택사항)
```

### 타입

| 타입 | 설명 |
|------|------|
| `feat` | 새로운 기능 추가 |
| `fix` | 버그 수정 |
| `docs` | 문서만 변경 (README, 주석 등) |
| `style` | 동작에 영향 없는 포맷/스타일 변경 |
| `refactor` | 기능 변경 없는 코드 개선 |
| `test` | 테스트 추가 또는 수정 |
| `chore` | 빌드, 설정, 패키지 관련 변경 |

### 규칙

- 제목은 **50자 이내**, 마침표 없이 작성
- 제목은 **현재형 동사**로 시작 (예: `추가`, `수정`, `제거`)
- 본문이 필요할 경우 **무엇을, 왜** 변경했는지 설명

### 예시

```
feat: CBS 알고리즘 충돌 감지 로직 추가

드론 간 충돌을 시간축 기준으로 감지하도록 구현.
기존 단순 거리 비교 방식의 한계를 개선함.
```

```
fix: 헝가리안 알고리즘 인덱스 오류 수정
```

```
docs: README에 환경 설치 방법 추가
```

---

## 커밋 템플릿 설정 (최초 1회)

```bash
git config commit.template .gitmessage
```

이후 `git commit` 실행 시 템플릿이 자동으로 에디터에 불러와집니다.
